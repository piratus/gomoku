/* eslint-env mocha */
/* global sinon */
import expect from 'expect.js';

import UserStore from '../UserStore';


describe('UserStore', ()=> {
  it('should have initial state', ()=> {
    expect(sinon).to.ok();
  });
});
